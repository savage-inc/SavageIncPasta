using UnityEngine;

public class PartyManager : MonoBehaviour {
    public Comfort comfortScript; // Access Comfort class

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update ()
    {
		// If character choosen
        // Then they are used for battle
        // If not choosen then they are left at the tarven to regain comfort
	}
}
